title: ALAWiT Test # Write here the name of your site
tagline: Test for a no-code creation of an Afro-Latin American in Translation MOREL website
img: logo.png # Add a file with a copy of your site's logo on /assets/img/tema
background: pattern.png # Add a file with a copy of your site's pattern on /assets/img/tema
image : avatar.png  # Add a file with a copy of your site's avatar on /assets/img/tema
email : sanchez@gmail.com
github : 
twitter : 
instagram:
remote_theme: morelrep/morel-theme
podcast:
jotform: https://form.jotform.com/211904785426056

# If you want to change the name of the repo, update the BASE_PATH name. Also, if you are going to use the MOREL repo as your main github page (e.g. `myuser.github.io`) comment this line
BASE_PATH: /morel-no-code-generator

# ADD AN X OR DELETE IT AND COMMIT IN ORDER TO UPDATE YOUR SITE

UPDATE: X

# Do not edit bellow unless you are an advanced user

exclude:
  - assets/env

plugins:
  - jekyll-seo-tag
  - jekyll-remote-theme

permalink: /:categories/:year/:month/:day/:title:output_ext 

excerpt_separator: "<!-- more -->"

collections:
  cities:
    output: true
    permalink: /cities/:name
  books:
    output: true
    permalink: /books/:name
  authors:
    output: true
    permalink: /authors/:name
  publishers:
    output: true
    permalink: /publishers/:name
  repositories:
    output: true
    permalink: /repositories/:name
  chriterias:
    output: true
    permalink: /chriteria/:name
  abouts:
    output: true

defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "books"
    values:
      layout: "book"
  - scope:
      path: ""
      type: "cities"
    values:
      layout: "city"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "publishers"
    values:
      layout: "publisher"
  - scope:
      path: ""
      type: "repositories"
    values:
      layout: "repository"
